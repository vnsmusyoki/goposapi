ALTER TABLE product_import_batch_rows
    ADD COLUMN IF NOT EXISTS name VARCHAR(255) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS sku VARCHAR(255) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS barcode VARCHAR(255) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS product_type VARCHAR(20) NOT NULL DEFAULT 'single',
    ADD COLUMN IF NOT EXISTS unit_id UUID NULL REFERENCES business_units(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS brand_id UUID NULL REFERENCES product_brands(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS category_id UUID NULL REFERENCES product_categories(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS sub_category_id INTEGER NULL REFERENCES product_sub_categories(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS is_for_selling BOOLEAN NOT NULL DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS manage_stock BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS alert_quantity INTEGER NULL,
    ADD COLUMN IF NOT EXISTS tax_type VARCHAR(20) NOT NULL DEFAULT 'exclusive',
    ADD COLUMN IF NOT EXISTS tax_rate NUMERIC(10,2) NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS default_purchase_price NUMERIC(14,4) NULL,
    ADD COLUMN IF NOT EXISTS purchase_price_exclusive NUMERIC(14,4) NULL,
    ADD COLUMN IF NOT EXISTS purchase_price_inclusive NUMERIC(14,4) NULL,
    ADD COLUMN IF NOT EXISTS profit_margin NUMERIC(10,2) NULL,
    ADD COLUMN IF NOT EXISTS default_selling_price NUMERIC(14,4) NULL,
    ADD COLUMN IF NOT EXISTS description TEXT,
    ADD COLUMN IF NOT EXISTS brochure_name VARCHAR(255),
    ADD COLUMN IF NOT EXISTS brochure_url TEXT,
    ADD COLUMN IF NOT EXISTS currency_code VARCHAR(20) NOT NULL DEFAULT 'USD',
    ADD COLUMN IF NOT EXISTS currency_symbol_placement VARCHAR(10) NOT NULL DEFAULT 'before',
    ADD COLUMN IF NOT EXISTS currency_precision INTEGER NOT NULL DEFAULT 2,
    ADD COLUMN IF NOT EXISTS all_locations BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS has_warranty BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS warranty_duration VARCHAR(50),
    ADD COLUMN IF NOT EXISTS warranty_period VARCHAR(20),
    ADD COLUMN IF NOT EXISTS warranty_coverage TEXT,
    ADD COLUMN IF NOT EXISTS created_by UUID REFERENCES users(id) ON DELETE SET NULL;
